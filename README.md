# RPEFluentManager

这个项目正在施工。

## 现在制作完毕的功能:

- 删除所有AutoSave文件
- 根据info.txt在pez内生成info.csv(需要补充曲绘作者)
- 一堆bug

## 将要制作的功能(饼):

- 将谱面置顶
- 导出pez并自动打包图片
- 谱面标识名更改
- 修复ChartList.txt(将RPE内删除的谱面还原)
- 短时父子线(不需要子线在全部谱面时间都绑定在父线上)
- 倍乘谱面BPM，不改变时间(适用于BPM填错，不过应该没什么用()
- 合并多层事件(也许做不出来)
- 少点bug

上述顺序不代表实现顺序。